pipeline {
    agent any

    stages {
        stage('Hello Stage') {
            steps {
                echo 'Hello Akash! Jenkins pipeline is working 🎉'
            }
        }
        stage('Checkworkspace') {
            steps {
                sh 'pwd'
            }
        }
    }
}
